<!DOCTYPE html>
@using Jonghor.ViewModel
@model RoomListViewModel
<html>
<style>
    h1 {
        text-align: center;
        font-size: 30px;
        color: Orange;
        font-weight: bold;
    }

    table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 90%;
        padding:50px
        
    }

    td, th {
        border: 2px solid #dddddd;
        text-align: left;
        padding: 20px;
    }


    tr:nth-child(even) {
        background-color: #dddddd;
    }
</style>
<article>
 
        <center><h1>Room Management</h1></center>
         <form action="/Dorm/Roomsort" method="post">
             <center>
                 <select name="option" onchange="this.form.submit()">
                     <option value="Avaliable">Avaliable</option>
                     <option value="Wait">Wait Roommate </option>
                     <option value="Not">Not Avaliable</option>
                     <option value="Reserve" select>Reserve Room</option>
                     <option value="All" select>All Room</option>
                     
                 </select>
             </center>
            

         </form>
           
    

   
        <center>
            <table>
                <tr>
                    <th width="10px"><center>Room Number</center></th>
                    <th>Name</th>
                    <th>Status</th>
                    <th>Person Reserve</th>
                    <th>Response</th>
                </tr>
                @foreach (RoomViewModel item in Model.Rooms)
                {
                    <tr>
                        <td>@item.RoomNo</td>
                        <td>
                            @foreach (string name in item.PeopleNames)
                            {
                                @name <br />                       
                            }
                        </td>
                        <td>@item.Status</td>
                        <td></td>
                        <td><center>
                                <form action="/Home/Index">
                                    <button type="submit" value="1" style="width:auto;">Accept</button>
                                    <button type="submit" value="0" style="width:auto;">Decline</button>
                                </form>
                            </center>
                        </td>
                    </tr>
                }


            </table>
        </center>

       
</article>
</html>

